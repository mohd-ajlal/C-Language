#include<stdio.h>

    int main(){
    char a;
    scanf("%c",&a);
    printf("%d",a);
    return 0;
}

// ascii value
// print alphabet from a to z using ascii value and for
// copy the array from an array
// to copy an array from another array and print them in reverse order
// to find second largest element in an array
// to check vowel constonent from alphabet