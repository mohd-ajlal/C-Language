#include<stdio.h>

    int main(){
    int side;
    printf("Enter side of equilateral triangle: ");
    scanf("%d", &side);
    printf("Area of equilateral triangle is: %f", 0.433*side*side);
    return 0;
}