// arithmetic operation in int data type

#include <stdio.h>

int main()
{
    int x = 10, y = 5;
    printf("x = %d, y = %d\n", x, y);
    printf("x +y: %d", x + y);
    printf("x-y: %d", x - y);
    printf("x*y: %d", x * y);
    printf("x/y: %d", x / y);
    return 0;
}